# Generate one fresh 32-byte base64 key. Never commit the populated .env file.
# Leave vendor credentials out of this file; setup collects them later.
CONFIG_ENCRYPTION_KEY=
# Optional: enables the HTTPS Zendesk article webhook endpoint.
WEBHOOK_SIGNING_SECRET=
